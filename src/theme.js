import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#31304D', // Example color
    },
    // Add your palette here
  }
  // You can add other theme customizations here
});

export default theme;
